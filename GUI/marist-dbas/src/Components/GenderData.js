const utils = require('../utils/utils.js');

const GenderData = [
    {
      id: utils.newID(),
      value: "M"
    },
    {
      id: utils.newID(),
      value: "F"
    },
    {
      id: utils.newID(),
      value: "Other"
    }
];

export default GenderData;
